package com.portalasig.ms.site.domain.entity;

/**
 * Utility class that defines constant path segments used across the PortalAsig application.
 * <p>
 * These constants are typically used to construct frontend URLs from backend services,
 * ensuring consistent routing between frontend and backend.
 * </p>
 */
public class RestPaths {

    /**
     * Contains path constants related to the frontend routing structure.
     */
    public static class FrontEnd {
        public static final String COURSE = "/asignatura";
    }
}
